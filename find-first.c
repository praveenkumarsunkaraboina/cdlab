printf("FIRST(T) = { ");
    for (int i = 0; i < tIndex; i++)
        printf("%c ", firstT[i]);
    printf("}\n");

    printf("FIRST(F) = { ");
    for (int i = 0; i < fIndex; i++)
        printf("%c ", firstF[i]);
    printf("}\n");

    return 0;
